from discord.ext.commands import bot
from utilities.config import bot_info
from utilities.gamedata import MinecraftProvider
from mcstatus import MinecraftServer

# server = MinecraftProvider(server["ip"], server["port"])
# server2 = MinecraftServer(host=server["ip"], port=server["port"])
# server.data_load()

# _dict = dict(zip())
# bot_info['guild_ids'], bot_info['permitted_ranks']
_dict = "null"

print(_dict)    